import React from "react";
import { Text, View } from "react-native";

const AlignmentScreen = () => {
	return (
		<View>
			<Text>Alignment Screen</Text>
		</View>
	);
};

export default AlignmentScreen;
